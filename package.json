{
  "name": "nahan-static",
  "version": "0.0.4",
  "description": "Static-files middleware for nahan.",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=html mocha",
    "coveralls": "nyc report --reporter=text-lcov | coveralls"
  },
  "repository": "LabMemNo003/nahan-static",
  "devDependencies": {
    "coveralls": "^3.1.0",
    "mocha": "^8.0.1",
    "nyc": "^15.1.0"
  },
  "dependencies": {
    "fs-extra": "^9.0.1"
  }
}
